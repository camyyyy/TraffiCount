// types/types.ts

export interface DailyData {
    day: string;
    count: number;
  }
  
  export interface MonthlyData {
    month: string;
    count: number;
  }

  // types/types.ts

export interface HourlyData {
    time: string;
    count: number;
  }
  